{
    "name": "root",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "pnpm --filter astro-live-compiler run start",
        "dev": "pnpm start",
        "build": "turbo run build --concurrency=1"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@types/node": "^18.18.9",
        "turbo": "^1.10.16"
    }
}
